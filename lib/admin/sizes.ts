export const HEADING_SIZE_CLS: Record<string, string> = {
  sm:   'text-[clamp(18px,1.6vw,28px)] md:text-[clamp(24px,2.2vw,36px)]',
  base: 'text-[clamp(22px,2vw,34px)] md:text-[clamp(32px,3vw,48px)]',
  lg:   'text-[clamp(26px,2.4vw,40px)] md:text-[clamp(36px,3.5vw,54px)]',
  xl:   'text-[clamp(30px,2.8vw,46px)] md:text-[clamp(42px,4vw,62px)]',
}

export const BODY_SIZE_CLS: Record<string, string> = {
  xs:   'text-xs md:text-sm',
  sm:   'text-sm md:text-base',
  base: 'text-base md:text-lg',
  lg:   'text-lg md:text-xl',
  xl:   'text-xl md:text-2xl',
}

export const TEXT_SIZE_CLS: Record<string, string> = {
  xs:  'text-xs',
  sm:  'text-sm',
  base: 'text-base',
  lg:  'text-lg',
  xl:  'text-xl',
  '2xl': 'text-2xl',
}
